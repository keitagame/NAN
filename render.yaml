services:
  - type: static
    name: nostr-client
    env: static
    plan: free
    buildCommand: npm install && npm run build
    publish: dist
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
